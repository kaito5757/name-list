import { render, screen } from "@testing-library/react";
import GlobalLayout from ".";
import "@testing-library/jest-dom";

describe("GlobalNavigation", () => {
  beforeEach(() => {
    render(<GlobalLayout></GlobalLayout>);
  });

  it("", () => {});
});
