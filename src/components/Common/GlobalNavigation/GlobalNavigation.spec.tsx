import { render, screen } from "@testing-library/react";
import GlobalNavigation from ".";
import "@testing-library/jest-dom";

describe("GlobalNavigation", () => {
  beforeEach(() => {
    render(<GlobalNavigation></GlobalNavigation>);
  });

  it("", () => {});
});
